# Local development API base URL.
# In Docker production, the Nginx proxy handles /api/ routing automatically,
# so this file is NOT used in production builds.
VITE_API_BASE_URL=http://localhost:8000/api/v1
